apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "gh-monit.fullname" . }}-config
  labels:
    {{- include "gh-monit.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.config }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
