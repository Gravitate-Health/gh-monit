apiVersion: v1
kind: Service
metadata:
  name: {{ include "gh-monit.fullname" . }}
  labels:
    {{- include "gh-monit.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "gh-monit.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
